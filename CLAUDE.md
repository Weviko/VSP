# VSP — 작업 규칙

베트남 체육 행정 플랫폼(VSP). 결정 근거는 `docs/01~13`, 실행 방법과 보안 경계는 `README.md`.

## 사업 주체와 소통하는 방식
- 답변은 한국어로 한다.
- 선택이 필요한 질문에는 **추천 방향과 위험성**을 함께 제시한다. 사업 주체가 오해 없이 고를 수 있게 하기 위해서다.
- 구조를 먼저 확정하고, 디자인은 문체부 협의 뒤 IT팀이 입힌다. 화면에는 구획과 순서만 둔다.
- **베팅(스포츠 토토)은 대외 문서·미팅 자료·공개 화면에 언급하지 않는다.** 정부 협력이 위태로워진다. 내부 문서에만 둔다.
- 사업 전체의 1번 병목은 개발이 아니라 **공식 서식 수령**이다. 서식은 `docs/received/` 에 원본 그대로 보관한다.

## 코드 경계 (위반하면 `npm test` 가 실패한다)
- `apps/portal`(대외 웹사이트)은 `@vsp/public-data`·`@vsp/web-shared` 만 import 하고, 서버 액션을 두지 않는다.
- 공개 규칙(출생연도만·미성년 보호·승인된 것만)은 `db/migrations/007_public_views.sql` 한 곳에만 둔다.
- 업무 화면은 `requireWorkspace`, 회원 화면은 `requireMember`, 서버 액션·API는 `workUserOrNull` 을 화면·함수마다 부른다.
- 개발 DB: `db:serve` 가 켜져 있으면 `db:setup`·`demo`·`test` 를 돌리지 않는다.

## 웹 조사와 브라우저 사용

**공통 정책은 `~/.claude/CLAUDE.md`(통합 웹·계정 작업 정책)를 따른다.** 사이트 등급(공개·로그인·차단·금지),
도구 선택, 확인이 필요한 행동, 차단 사이트의 사람 경유 방법, 비밀값 규칙이 모두 거기 있다. 여기에 다시 적지 않는다.

VSP 에만 해당하는 것:
- VSP 대외 웹사이트의 데이터는 협회 입력과 공식 기록 제공처 계약으로만 채운다. 네이버 등 다른 서비스에서 가져오지 않는다.
- 벤치마크 조사 결과는 `docs/` 해당 문서에 출처 URL·확인 날짜와 함께 기록한다.
- 베트남 정부 시스템(국가 공공서비스 포털·VNeID)은 조사만 하고, 제출·로그인은 사업 주체가 직접 한다.
